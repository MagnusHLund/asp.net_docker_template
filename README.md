# Setup ASP.net project

## Run setup script

Run the bash script, buildTemplate.sh
The script takes a parameter, which is the name that your project has.
The script requires sudo.

`sudo bash buildTemplate.sh MyProjectName`

## TODO

Ensure ChangeMe gets correctly translated to a custom name, based on file
Might be best to use snake case to determine correctly determine the name

For production secrets, use appsettings.Production.json

Is it possible to force a process to have a specific process id?
